<%= form_with(model: capacity) do |form| %>
  <% if capacity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(capacity.errors.count, "error") %> prohibited this capacity from being saved:</h2>

      <ul>
        <% capacity.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :sector_id %>
    <%= form.text_field :sector_id %>
    <br><br>
  </div>

  <div class="field">
    <%= form.label :percentage %>
    <%= form.text_field :percentage %>
    <br><br>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
    <br><br>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
